Broaden the tiny runtime-parsed RPG-II subset by adding one more constrained behavior without turning it into a general executor: either add a second simple parsed C-spec operation over the current field/output path, or add one parsed indicator-style gate that enables one of the existing output definitions, then update the demo surface/docs/tests with a variant that exercises the new behavior.
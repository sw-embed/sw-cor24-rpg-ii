Broaden the tiny runtime-parsed RPG-II subset by adding one more small but real composition capability on top of the current MOVE/REVR calc subset. Prefer either a second parsed gate condition on output selection or a tiny two-step calc chain, whichever stays smaller and keeps the vendored HLASM path stable. Add one new source fixture that exercises the new behavior end to end, keep the existing demos green, and update CLI/docs/regression surfaces only as needed.
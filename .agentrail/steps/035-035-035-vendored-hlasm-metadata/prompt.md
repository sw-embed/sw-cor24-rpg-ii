Make the vendored HLASM workflow explicit and reproducible in this repo. Add a checked-in metadata file that records the upstream sw-cor24-hlasm repo, the vendored commit SHA, the vendored source path, the refresh command, and the build precedence so a fresh clone can rebuild or refresh the pinned tool snapshot. Update build/docs only as needed, keep the vendored dependency itself unmodified, and validate the repo still builds/tests against the vendored snapshot.
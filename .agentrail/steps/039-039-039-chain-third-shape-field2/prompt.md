Broaden the tiny runtime-parsed RPG-II subset by extending the chained calc path one more small step without destabilizing the pinned vendored HLASM path. Prefer one small chained behavior that composes calc slot 1 with the third output shape but changes which field or selector drives the visible result, add one new source fixture that exercises it end to end, and update CLI/docs/regression surfaces only as needed.
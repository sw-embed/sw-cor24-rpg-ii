Implemented _read_record subroutine with deck descriptor (base, length, count, index). Added _emit_char (with TX busy-wait), _emit_10 (print 10 bytes), _emit_crlf helpers. Fixed COR24 calling convention issues: fp+offset for args depends on number of saved registers, and descriptor pointer must be preserved across register reuse. Test deck of 3 records loaded via --load-binary, reg-rs test rpg2_deck_reader passes.